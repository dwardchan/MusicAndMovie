import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

const state = {
  LoginMessage: String,
};
export default new Vuex.Store({
  state,
  mutations: {},
  actions: {},
  modules: {},
});
